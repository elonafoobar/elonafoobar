locale {
    mef {
        bomb_counter = "*${_1}*"

        melts = "${name(_1)} melt${s(_1)}."
        is_burnt = "${name(_1)} ${is(_1)} burnt."
        steps_in_pool = "${name(_1)} step${s(_1)} in the pool."
        destroys_cobweb = "${name(_1)} destroy${s(_1)} the cobweb."
        is_caught_in_cobweb = "${name(_1)} ${is(_1)} caught in a cobweb."
        attacks_illusion_in_mist = "${name(_1)} attack${s(_1)} an illusion in the mist."
    }
}
